[package]
name = "blsync-desktop"
version = "0.6.0"
description = "BLSync desktop application"
authors = ["oxygenkun"]
edition = "2021"

[lib]
name = "blsync_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
