# UID and GID for file permissions
# 默认值: 1000
# 设置为你当前用户的 UID 和 GID，避免容器内创建的文件权限问题
#
# 查看当前用户的 UID 和 GID:
#   id -u    # 查看 UID
#   id -g    # 查看 GID
#
# 示例:
#   UID=1000
#   GID=1000

UID=1000
GID=1000

# 后端 API 配置
# 用于指定后端服务器的地址
#
# 开发环境: Vite 开发服务器会将 API 请求代理到该地址
# 生产环境: 前端代码运行时会使用该地址发送 API 请求
#
# 说明:
#   - 如果设置为相对路径如 /api 或空值，前端将与页面同源请求
#   - 如果设置为完整 URL，前端将请求指定地址
#
# 示例:
#   VITE_API_BASE_URL=http://localhost:8000          # 开发环境默认
#   VITE_API_BASE_URL=/api                           # 使用相对路径（生产环境推荐）
#   VITE_API_BASE_URL=http://192.168.1.100:8000/api # 局域网地址
#   VITE_API_BASE_URL=https://api.example.com/api   # 远程地址

VITE_API_BASE_URL=/api
